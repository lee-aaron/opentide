apiVersion: v1
kind: Service
metadata:
  name: {{ include "opentide.fullname" . }}
  labels:
    {{- include "opentide.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opentide.selectorLabels" . | nindent 4 }}
